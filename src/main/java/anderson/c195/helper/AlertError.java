package anderson.c195.helper;
/** This is an interface for AlertError.
 * Used for creating a lambda expressions throughout program.
 * */
@FunctionalInterface
public interface AlertError {
    void alertError(String e);
}
